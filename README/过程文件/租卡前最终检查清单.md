# ESField 租卡前最终检查清单

检查日期：2026-05-16

## 一、代码与测试

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 全部源码编译 | 通过 | `compileall src scripts` 0 错误 |
| 单元测试 | 9/9 通过 | PyTorch test 正常运行 |
| 8 个 bug 修复 | 已修复 | 详见 代码审查与修复记录.md |
| 新增文件 | 5 个 | `paflow_atom_mapping.py`, `adapt_paflow.py`, `run_esfield_paflow_pipeline.py` + 2 个过程文档 |

## 二、外部依赖

| 依赖 | 版本 | 用途 | 状态 |
|------|------|------|------|
| **PyTorch** | 2.7.0+cu128 | 训练+guidance | 已安装 (CPU可用) |
| **torch_geometric** | 2.7.0 | PAFlow GNN | 已安装 |
| **torch_scatter** | 2.1.2+pt27cu128 | PAFlow scatter ops | 已安装 |
| **RDKit** | 2026.3.1 | QED, SA, 分子重建 | 已安装 |
| **openbabel** | 3.1.1.23 (wheel) | PAFlow 分子重建 | 已安装 |
| **scipy** | 1.17.1 | PAFlow 距离计算 | 已安装 |
| **lmdb** | 2.2.0 | PAFlow 数据集读取 | 已安装 |
| **matplotlib** | 3.10.3 | 3D 散点图 | 已安装 |
| **pyKVFinder** | - | 口袋体积/面积计算 | 已安装 |
| **fpocket** | 4.0 | 疏水空腔检测 | 从源码编译完成 |

## 三、PAFlow Base Generator

| 检查项 | 结果 | 路径 |
|--------|------|------|
| 预训练 flow 模型 | 32 MB | `/root/PAFlow-main/pretrained_models/pretrained_flow.pt` |
| 原子数预测模型 | 808 KB | `/root/PAFlow-main/pretrained_models/atom_num_model.pt` |
| CrossDocked lmdb | 4.1 GB | `/root/PAFlow-main/data/crossdocked_v1.1_rmsd1.0_pocket10_processed_final.lmdb` |
| 数据 split | 425 KB | `/root/PAFlow-main/data/crossdocked_pocket10_pose_split.pt` |
| 原子数数据集 | 11 MB | `/root/PAFlow-main/data/atom_num_dataset.pkl` |
| Test set (100 口袋) | - | `/root/PAFlow-main/data/test_set/` |
| PAFlow 模型导入 | 成功 | `from models.molopt_score_model_guide import ScorePosNet3D_guided_flow` |
| ESField↔PAFlow 类型映射 | 已验证 | 11 种 ESField → 13 种 PAFlow |

## 四、PDB 数据集与 Crystal Water

| 检查项 | 结果 | 说明 |
|--------|------|------|
| PDBbind 数据 | 3.2 GB | `/root/autodl-tmp/data/PDB/P-L.tar.gz` |
| 含 crystal water | 确认 | 10gs 案例: 169 waters |
| Crystal water site detection | 通过 | 169 water → 13 candidates → 4 merged sites |
| fpocket 疏水空腔检测 | 通过 | 10gs: 20 fpocket pockets → 16 merged sites |
| 合并 site map | 通过 | 20 total sites (2 stable, 2 high_energy, 16 hydrophobic) |

## 五、ESField-PAFlow 集成

| 检查项 | 状态 | 文件 |
|--------|------|------|
| 原子类型映射 | 已完成 | `src/generation/paflow_atom_mapping.py` |
| Guidance 注入方案 | 已完成 | `src/generation/adapt_paflow.py` |
| 端到端 pipeline 脚本 | 已完成 | `scripts/run_esfield_paflow_pipeline.py` |
| 注入代码落地 | **待租卡后** | 需在 GPU 上实测 PAFlow 采样循环 |

## 六、租卡前缺失（仅剩 2 项）

| # | 项目 | 说明 |
|---|------|------|
| 1 | **GPU 硬件** | 当前 `torch.cuda.is_available() = False`。租卡后 PyTorch 可直接切换 |
| 2 | **ESField 注入实测** | 注入方案已文档化，但需 GPU + PAFlow checkpoint 才能实测 |

## 七、租卡后第一组命令

```bash
# 1. 验证 GPU
python -c "import torch; assert torch.cuda.is_available()"

# 2. 验证 PAFlow 模型可加载
cd /root/PAFlow-main
python -c "
import torch
from models.molopt_score_model_guide import ScorePosNet3D_guided_flow
ckpt = torch.load('./pretrained_models/pretrained_flow.pt', map_location='cuda:0')
print('Checkpoint loaded, keys:', list(ckpt.keys())[:10])
"

# 3. 运行 site map 构建（CPU 可行）
cd /root/ESField
PYTHONPATH=src python scripts/run_esfield_paflow_pipeline.py \
  --protein-pdb /tmp/P-L/1981-2000/10gs/10gs_protein.pdb \
  --ligand-sdf /tmp/P-L/1981-2000/10gs/10gs_ligand.sdf \
  --protein-id 10gs \
  --output-dir experiments/esfield_paflow/10gs \
  --mode prepare

# 4. PAFlow baseline 生成（需要 GPU）
cd /root/ESField
PYTHONPATH=src python scripts/run_esfield_paflow_pipeline.py \
  --protein-pdb /tmp/P-L/1981-2000/10gs/10gs_protein.pdb \
  --protein-id 10gs \
  --output-dir experiments/esfield_paflow/10gs \
  --volume 300 --area 300 \
  --mode baseline

# 5. ESField guided generation（需要注入代码修改后）
# 修改 /root/PAFlow-main/models/molopt_score_model_guide.py 第 703 行后
# 注入 ESField site gradient，详见 src/generation/adapt_paflow.py
```

## 八、结论

**租卡前的所有能做的准备工作已 100% 完成。** 代码质量、依赖安装、数据准备、外部工具、集成方案全部就绪。租卡后仅需：
1. 验证 GPU 可用
2. 加载 PAFlow checkpoint
3. 将 ESField 注入代码落地到 PAFlow 采样循环（注入位置和代码已完全文档化）
4. 跑 guided vs unguided 对比实验
