# ESField 实验运行记录

更新时间：2026-06-04

这个文件记录真实运行过的命令。没有运行过的结果不得写成已完成。

## 运行前检查清单

- [ ] 是否需要 GPU？不需要则不要开付费 GPU。
- [ ] 预计是否超过 10 分钟？超过则必须使用 `tmux`。
- [ ] 是否记录了 config、命令、输出目录和随机种子？
- [ ] 是否确认数据路径存在？
- [ ] 是否确认输出目录不会覆盖旧结果？
- [ ] 是否设置云平台自动关机或人工关机提醒？

## 记录模板

```text
日期：
机器：
GPU：
tmux session：
git commit：
任务目的：
配置文件：
命令：
数据输入：
输出目录：
随机种子：
开始时间：
结束时间：
耗时：
是否设置关机：
结果摘要：
失败原因：
后续动作：
```

## 运行记录

### 2026-06-04 — v7.1 全部实验

```text
日期：2026-06-04
机器：Linux 服务器 (autodl), RTX 4090D 24GB
GPU：使用（DrugFlow 采样，~0.07 GB Phase1, ~0.05 GB Phase2）
CPU：使用（Vina 对接 × 4 workers）
tmux session：不适用
任务目的：v7.1 完整实验 (10 口袋生成 + 消融 + 多样性 + Vina 对接)
输出目录:
  - experiments/pdbbind_water_sites/v71_ablation/    (消融)
  - experiments/pdbbind_water_sites/v71_full_study/  (baseline+v7.1)
  - experiments/phase2b_results/                     (Vina)
  - paper/                                           (论文材料)
耗时：
  - Phase1 (10 pockets): ~15s/pocket
  - Phase2 (25 samples): ~10s/pocket
  - Ablation (10×6): ~700s total
  - Vina full dock (9 pockets): ~100-760s/pocket
结果摘要：
  - 7/10 口袋 DirectOcc > 0%（v6-D.2 baseline = 0%）
  - v7.1 vs baseline: DirectOcc 15.1% mean, Vendi +27-45%
  - 占据组 Vina 得分不优于未占据组
  - 76/76 测试通过
失败原因：3fzn Phase1 失败（所有条件）；1h0r/3t09 DirectOcc=0%
后续动作：论文撰写
```

### 2026-05-11

- 暂无训练、生成或评估运行记录。当前仅完成文档和开发规范准备。

### 2026-05-14

本日运行无卡代码验证（代码审查修复）和 toy pipeline，未运行真实训练、真实生成、docking、PoseBusters 或 GPU 作业。

```text
日期：2026-05-14
机器：Linux 服务器 (autodl)
GPU：未使用
tmux session：不适用
git commit：未提交
任务目的：代码审查后修复 8 个 bug，验证编译、测试和 toy pipeline
配置文件：configs/mvp.yaml
命令：
  python -m compileall src scripts
  PYTHONPATH=src python -m unittest discover -s tests
  PYTHONPATH=src python scripts/run_toy_pipeline.py --output-dir /tmp/toy_pipeline_test --overwrite
数据输入：toy pipeline 内置数据
输出目录：/tmp/toy_pipeline_test
随机种子：20260511
开始时间：未记录
结束时间：未记录
耗时：测试 15s，toy pipeline 秒级
是否设置关机：否
结果摘要：
  - compileall: 全部通过，0 错误
  - unittest: 9 个测试全部通过（PyTorch test 在无 PyTorch 环境自动 skip）
  - toy pipeline: 成功生成 11 个输出文件
  - 修复的 8 个问题：2 严重（拼写错误导致 CLI 参数无效、target_count 计算逻辑错误）、3 中等（加权质心不精确、空蛋白边界、guided gen 错误信息）、2 轻微（source 命名、atomic_number 语义）、1 文档（USAGE.md Windows 路径）
失败原因：无
后续动作：确定 pocket-conditioned base generator、安装依赖、解压数据、租卡跑真实 smoke
```

```text
日期：2026-05-14
机器：Linux 服务器 (autodl)
GPU：未使用
tmux session：不适用
git commit：未提交
任务目的：生成全文件速查表和过程文档
配置文件：不适用
命令：仅文档写入，无运行命令
数据输入：不适用
输出目录：README/过程文件/
随机种子：不适用
开始时间：未记录
结束时间：未记录
耗时：不适用
是否设置关机：否
结果摘要：生成 代码文件速查表.md（含每个文件的技术详解）和 代码审查与修复记录.md；更新 任务看板.md、开发者文档.md、实验运行记录.md
失败原因：无
后续动作：同上
```

```text
日期：2026-05-14
机器：Linux 服务器 (autodl)
GPU：未使用（无 GPU 驱动，CPU only）
tmux session：不适用
git commit：未提交
任务目的：租卡前环境准备和真实 CrossDocked 数据 smoke test
配置文件：configs/mvp.yaml
命令：
  pip install rdkit
  # 抽样解压 3 个 CrossDocked 案例
  cd /root/ESField && PYTHONPATH=src python scripts/index_crossdocked_archive.py --archive /root/autodl-tmp/data/downsampled_CrossDocked2020_v1.3.tgz --limit 18 --extract --extract-dir data/raw/crossdocked_sample
  # 对每个案例运行完整 pipeline
  PYTHONPATH=src python -m site_detection.build_crystal_water_sites ...
  # 合并 site map + 构建 pairs
  # CPU 训练 CompatibilityPotential（32 pairs, 30 epochs）
  PYTHONPATH=src python -m training.train_potential --train-pairs data/atom_site_pairs/train_pairs.jsonl --valid-pairs data/atom_site_pairs/valid_pairs.jsonl --output-dir experiments/real_data_smoke/potential_3case --epochs 30 --device cpu
  # 分子质量评估
  PYTHONPATH=src python -m evaluation.molecule_quality --inputs data/raw/crossdocked_sample/*_uff2.sdf --output-json experiments/real_data_smoke/all_quality.json
  # 可视化
  PYTHONPATH=src python -m unittest discover -s tests
数据输入：3 个真实 CrossDocked 案例 (CBP_HUMAN, DYR_HUMAN, JAK2_HUMAN)
输出目录：experiments/real_data_smoke/
随机种子：20260511
开始时间：未记录
结束时间：未记录
耗时：训练 30 epochs CPU 约 2 分钟；索引/解压约 30 秒
是否设置关机：否
结果摘要：
  - 依赖：PyTorch 2.7.0+cu128 (CPU)、RDKit 2026.3.1 安装成功
  - fpocket 无法安装（apt/conda/pip 均不可用）
  - Crystal water：3 个受体均无 crystal water（CrossDocked 预处理时已被移除）
  - 真实数据 pipeline 全部跑通：site detection → merge → pair build → site matching → quality → visualization
  - CPU 势能网络训练：32 pairs, AUC 从 0.4 → 1.0, ranking_accuracy 0.4 → 1.0, mean_pos_energy -4.26, mean_neg_energy +4.30
  - RDKit 分子质量：3 个配体 QED 分别为 0.314/0.335/0.273
  - 9 个单元测试全部通过
  - Checkpoint 加载验证通过
失败原因：无；fpocket 和 sascorer 未安装，代码优雅降级处理
风险发现：
  1. CrossDocked 受体均无 crystal water，水相关 site detection 无法工作 → 需从原始 PDB 获取水坐标或用水预测工具
  2. fpocket 不在标准包管理器中 → 需源码编译
  3. 当前机器无 GPU → 租卡后需重配 CUDA 环境
后续动作：租卡后安装 fpocket、决定 water 策略、批量处理 100+ 案例、GPU 训练
```
